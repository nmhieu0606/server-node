import { Express } from 'express';
import { RouteMetaData, ExpressRegex, Key } from '../types';
/**
 * Parses an Express app and generates list of routes with meta data
 *
 * @param app The Express app reference. Must be used after all routes have been attached
 * @returns List of routes for this express app with meta-data that has been picked up
 */
export declare const parseExpressApp: (app: Express) => RouteMetaData[];
export declare const onlyForTesting: {
    pathRegexParser: (layerRegexPath: ExpressRegex | string, keys: Key[]) => string;
    mapKeysToPath: (layerRegexPath: ExpressRegex, keys: Key[]) => string;
};
